# Local secrets — copy to .env and fill in. NEVER commit .env.
#
# OpenRouter routes to DeepSeek, Mistral, Qwen, Llama, GPT, Claude, etc.
# through one OpenAI-compatible endpoint. Get a key at
# https://openrouter.ai/keys — free credits available; set a hard
# spend cap in the dashboard before using it for orchestration.

OPENROUTER_API_KEY=sk-or-v1-...
