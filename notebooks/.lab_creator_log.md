# Lab Creator — Scheduled Run Log

## 2026-05-01 · ABORT (no commit, no push)

**Topic read from queue:** `logistic regression` (first non-blank, non-comment line of `notebooks/lab_topic_queue.txt`).

**Result:** Aborted before any file changes. Working tree left untouched.

### Reasons (either is sufficient)

1. **Slug conflict.** Topic resolves to slug `logistic-regression`, but `work/logistic-regression-lab/` already exists on disk and is wired into the homepage labs grid + nav dropdown (FIG. 12). Building this topic would clobber an existing lab. Per task instructions: "Do not retry the same topic — leave it in the queue for me to inspect."

2. **Dirty working tree.** Pre-existing uncommitted changes detected:
   - **Modified** (14): `index.html`, `work/ab-test-lab/index.html`, `work/agi-forecast-lab/index.html`, `work/churn-lab/index.html`, `work/funnel-sim-lab/index.html`, `work/jobs-lab/index.html`, `work/logistic-regression-lab/index.html`, `work/model-atlas-lab/index.html`, `work/model-picker-lab/index.html`, `work/reco-lab/index.html`, `work/segmentation-lab/index.html`, `work/sketch-lab/index.html`, `work/time-use-lab/index.html`, `work/watchdog-lab/index.html`
   - **Untracked**: `assets/css/escape-velocity-lab.css`, `assets/css/model-card.css`, `assets/data/escape-velocity/`, `assets/js/decision-tree-lab.js`, `assets/js/escape-velocity-lab.js`, `assets/js/kmeans-lab.js`, `assets/js/linear-regression-lab.js`, `assets/js/random-forest-lab.js`, `work/decision-tree-lab/`, `work/escape-velocity-lab/`, `work/kmeans-lab/`, `work/linear-regression-lab/`, `work/random-forest-lab/`

   Auto-committing on top of this would bundle in-flight manual work under a `lab: add ... [auto, scheduled run]` message and almost certainly fail the QA gate (cross-cutting nav inconsistencies between the queue's existing labs and the in-progress ones).

### Queue drift detected

The queue still lists topics that already shipped as labs:

| Queue line | Existing lab |
|---|---|
| `logistic regression` | `work/logistic-regression-lab/` (FIG. 12) |
| `k-means clustering` | `work/kmeans-lab/` |
| `random forest feature importance` | `work/random-forest-lab/` |

The queue was apparently never pruned after those labs were built (likely interactively, not via this scheduled job). Recommend pruning these three lines so the next scheduled run picks up `markov chain` (the first topic without a corresponding lab on disk).

### Actions taken

- None. No files modified, no commits, no push.
- This log file appended/created.

### Suggested next step (for the user, interactively)

1. Commit or stash the in-flight changes in the working tree.
2. Prune already-built topics from `notebooks/lab_topic_queue.txt`:

   ```diff
   - logistic regression
     markov chain
   - k-means clustering | live demo on user-uploaded points
   - random forest feature importance
     PCA projection
     ...
   ```

3. Re-run the scheduled task. With a clean tree and a pruned queue, it will build `markov chain` as FIG. (next available).

— end entry —

## 2026-05-02 · ABORT (no commit, no push) — repeat of 2026-05-01

**Topic read from queue:** `logistic regression` (still the first non-blank, non-comment line of `notebooks/lab_topic_queue.txt`).

**Result:** Aborted before any file changes. Working tree left untouched. Queue file left untouched.

### Why this run aborted

This is the second consecutive scheduled run that hit the same wall. Nothing has changed since 2026-05-01:

1. **Slug still conflicts.** `work/logistic-regression-lab/index.html` exists on disk (FIG. 12), is referenced from the homepage labs grid, and is listed in every existing lab's nav dropdown. Building this topic now would clobber a shipped lab. Per task instructions: "Do not retry the same topic — leave it in the queue for me to inspect."

2. **Working tree still dirty.** `git status --porcelain` reports three modified files (with iCloud "Resource deadlock avoided" read errors during indexing — these are FUSE/iCloud sync artifacts, not git problems, but they confirm files are mid-sync and unsafe to bundle into an auto-commit):
   - `assets/css/imm-lab.css`
   - `assets/data/escape-velocity/metr-horizon.json`
   - `notebooks/imm/RUNLOG.md`

   Last commit on `main`: `eeb942c main.css: cache-bump v=26→v=27 (catch pages missed in last sweep)`. The lab dirs from the previous log entry's "Untracked" list (decision-tree, escape-velocity, kmeans, linear-regression, random-forest) appear to have been committed in the interim — good. But the three above are still pending.

### Queue drift — unchanged from yesterday

Topics in the queue that already correspond to shipped labs:

| Queue line | Existing lab on disk |
|---|---|
| `logistic regression` | `work/logistic-regression-lab/` (FIG. 12) |
| `k-means clustering` | `work/kmeans-lab/` |
| `random forest feature importance` | `work/random-forest-lab/` |

The next scheduled run will abort again unless the queue is pruned.

### Actions taken this run

- None to git. No files modified except this log file.
- Queue file left untouched (per "leave it in the queue for me to inspect").
- This entry appended.

### Suggested next step (manual, one-time)

Edit `notebooks/lab_topic_queue.txt` and remove these three lines:

```diff
- logistic regression
  markov chain
- k-means clustering | live demo on user-uploaded points
- random forest feature importance
  PCA projection
  ...
```

After that, the next scheduled run will pick up `markov chain` as the topic and assign it the next available FIG number. Also commit or stash the three pending files above so the auto-commit at the end of a successful run isn't polluted with unrelated work.

— end entry —

## 2026-05-03 · ABORT (no commit, no push) — third consecutive same-topic abort

**Topic read from queue:** `logistic regression` (still the first non-blank, non-comment line of `notebooks/lab_topic_queue.txt`).

**Result:** Aborted before any file changes other than appending this log entry. No commits. No push. Queue file left untouched.

### Why this run aborted (same wall as 2026-05-01 and 2026-05-02)

1. **Slug still conflicts.** `work/logistic-regression-lab/index.html` exists (FIG. 12), is referenced from the homepage labs grid, and is in every existing lab's nav dropdown. Building this topic would clobber a shipped lab. Per task instructions: "Do not retry the same topic — leave it in the queue for me to inspect."

2. **Working tree still dirty.** `git status --porcelain` reports (with the same iCloud "Resource deadlock avoided" indexing errors as yesterday — sync is mid-flight, unsafe to bundle into an auto-commit):
   - `M assets/css/imm-lab.css`
   - `M assets/data/escape-velocity/metr-horizon.json`
   - `M notebooks/.lab_creator_log.md` (this file — pending the previous run's append)
   - `M notebooks/imm/RUNLOG.md`

   Last commit on `main`: `eeb942c main.css: cache-bump v=26→v=27 (catch pages missed in last sweep)`. No new commits since the 2026-05-02 run, which means the previous abort log was never committed either.

### Queue drift — unchanged from the last two runs

| Queue line | Existing lab on disk |
|---|---|
| `logistic regression` | `work/logistic-regression-lab/` (FIG. 12) |
| `k-means clustering` | `work/kmeans-lab/` |
| `random forest feature importance` | `work/random-forest-lab/` |

The next scheduled run will abort again on the same `logistic regression` line until the queue is pruned manually.

### Actions taken this run

- None to git. No files modified except this log file.
- Queue file left untouched (per "leave it in the queue for me to inspect").
- This entry appended.

### Suggested next step (manual, one-time)

1. Commit or stash the three pending files (`assets/css/imm-lab.css`, `assets/data/escape-velocity/metr-horizon.json`, `notebooks/imm/RUNLOG.md`) so the next auto-commit isn't polluted with unrelated work.
2. Edit `notebooks/lab_topic_queue.txt` and remove the three already-built lines:

   ```diff
   - logistic regression
     markov chain
   - k-means clustering | live demo on user-uploaded points
   - random forest feature importance
     PCA projection
     ...
   ```

3. Commit the pruned queue and log.
4. The next scheduled run will then pick up `markov chain` as the topic and assign it the next available FIG number.

— end entry —

## 2026-05-04 · ABORT (no commit, no push) — fourth consecutive same-topic abort

**Topic read from queue:** `logistic regression` (still the first non-blank, non-comment line of `notebooks/lab_topic_queue.txt`).

**Result:** Aborted before any file changes other than appending this log entry. No commits. No push. Queue file left untouched.

### Why this run aborted (same wall as 2026-05-01, 2026-05-02, 2026-05-03)

1. **Slug still conflicts.** `work/logistic-regression-lab/index.html` exists (FIG. 12, 20290 bytes — a fully shipped lab, not a stub), is referenced from the homepage labs grid, and is in every existing lab's nav dropdown. Building this topic would clobber a shipped lab. Per task instructions: "Do not retry the same topic — leave it in the queue for me to inspect."

2. **Working tree is clean this time.** `git status --porcelain` returns empty — the four pending files flagged in the previous three abort entries have been resolved. Last commit on `main`: `6ef8b19 premium-vibe pass 2: stagger labs grid, drop-cap parity, editorial 404`. So the only thing still blocking is the queue itself.

### Queue drift — unchanged from the last three runs

| Queue line | Existing lab on disk |
|---|---|
| `logistic regression` | `work/logistic-regression-lab/` (FIG. 12) |
| `k-means clustering` | `work/kmeans-lab/` |
| `random forest feature importance` | `work/random-forest-lab/` |

The next scheduled run will abort again on the same `logistic regression` line until the queue is pruned manually.

### Actions taken this run

- None to git. No files modified except this log file.
- Queue file left untouched (per "leave it in the queue for me to inspect").
- This entry appended.

### Suggested next step (manual, one-time)

Edit `notebooks/lab_topic_queue.txt` and remove the three already-built lines:

```diff
- logistic regression
  markov chain
- k-means clustering | live demo on user-uploaded points
- random forest feature importance
  PCA projection
  ...
```

After that, the next scheduled run will pick up `markov chain` as the topic and assign it the next available FIG number (highest existing FIG is 16, so the new lab would be FIG. 17 — but verify against the homepage grid before relying on that, since several labs added since this skill was first written are not numbered in the FIG. NN section).

— end entry —

## 2026-05-05 · ABORT (no commit, no push) — fifth consecutive same-topic abort

**Topic read from queue:** `logistic regression` (still the first non-blank, non-comment line of `notebooks/lab_topic_queue.txt`).

**Result:** Aborted before any file changes other than appending this log entry. No commits. No push. Queue file left untouched.

### State this run

- **Working tree:** clean. `git status --porcelain` returns empty.
- **Last commit on `main`:** `305f072 seo polish: trim long meta descriptions, add article:* tags, BreadcrumbList`.
- **Conflict:** `work/logistic-regression-lab/index.html` still exists (FIG. 12, 23770 bytes — has grown since the 2026-05-04 abort, so it is being actively maintained, not abandoned). Building this topic would clobber a shipped lab.

The only thing still blocking is the queue. Per task instructions: "Do not retry the same topic — leave it in the queue for me to inspect." Same wall as 2026-05-01 through 2026-05-04. This is the fifth consecutive day. The queue has not been pruned and the scheduled run continues to read `logistic regression` first.

### Queue drift — unchanged from the last four runs

| Queue line | Existing lab on disk |
|---|---|
| `logistic regression` | `work/logistic-regression-lab/` (FIG. 12) |
| `k-means clustering` | `work/kmeans-lab/` |
| `random forest feature importance` | `work/random-forest-lab/` |

### Note on log delivery

If you have not seen the prior abort entries, this log file may be the issue — it lives in `notebooks/.lab_creator_log.md` (leading dot, easy to miss in finders/listings). The scheduled task does not surface the log content anywhere else. Consider checking it directly the next time the auto-run does not produce a commit.

### Actions taken this run

- None to git. No files modified except this log file.
- Queue file left untouched.
- This entry appended.

### Single manual step that unblocks every future run

Open `notebooks/lab_topic_queue.txt` and delete these three lines:

```
logistic regression
k-means clustering | live demo on user-uploaded points
random forest feature importance
```

The next scheduled run will then pick up `markov chain` and assign it FIG. 17 (after confirming against the homepage grid).

— end entry —
