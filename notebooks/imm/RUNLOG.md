# IMM Lab — GCP setup run log

Run date: 2026-05-01
Run by:   Claude Code (Opus 4.7) on user's Mac
Elapsed:  ~25 min wall-clock (most of it pip install + first cloud build)

## Resources

- **Project:** `cuth-imm-lab` (project number `290331859515`)
- **Billing account:** `011FB8-B9D990-00222D`, billing enabled
- **Budget:** *not set programmatically — recommend setting $5/mo cap
  with 50/90/100% alerts at https://console.cloud.google.com/billing/budgets*
- **APIs enabled:** bigquery, storage, run, cloudbuild, artifactregistry
- **BigQuery:** `cuth-imm-lab.imm_lab.weekly_panel`, 104 rows
- **Cloud Storage:** `gs://cuth-imm-lab-artifacts`, lifecycle = delete after 90 days
  - Object: `imm/imm_model.pkl` (PyMC trained model)
- **Cloud Run service:** `imm-lab-api`, region `us-central1`
  - URL: `https://imm-lab-api-290331859515.us-central1.run.app`
  - min=0 max=1 memory=512Mi cpu=1 concurrency=20
  - Image: `gcr.io/cuth-imm-lab/imm-lab-api:latest`
    digest: `sha256:4070170b56ca59b62f2d3e28176a54cb13c264e8c1730a1611b5ced167974586`
  - Env vars: `IMM_GCS_BUCKET`, `IMM_RATE_LIMIT_PER_MIN=10`, `IMM_CORS_ORIGINS`

## Diagnostics from this run

- **PyMC version:** 5.28.5
- **Sampler:** NUTS, 4 chains × 1,000 warmup × 1,000 draws (4,000 + 4,000 total)
- **Wall-clock sampling time:** 20 seconds
- **rhat_max:** 1.003 (target < 1.05 — passed)
- **ess_min:** 1,482 (target > 400 — passed)
- **Divergent transitions:** 1
- **Recovered in 90% CI:** 7 / 8 channels (expected at nominal 90%: 7.2)
  - Single miss: `retail_media` (true α $42k, 90% CI [$45.6k, $192.6k])
  - Reason: Hill+adstock spec doesn't carry the synth's
    `retail_media × peak-retail` interaction; α absorbs the variance and
    the lower CI shifts up. Adding the interaction term would close it.

## Smoke tests

- `GET https://imm-lab-api-290331859515.us-central1.run.app/health`
  → `200 {"status":"ok"}`
- `POST /predict` with current allocation (8 channels at documented mean spend)
  → `200 {"mean":473902.12, "lo":433330.45, "median":474230.10, "hi":522940.31, "library":"PyMC 5.28.5 (Meridian fallback)"}`

## Notes

- Renamed `/healthz` → `/health` mid-run because Google's global frontend
  reserves `/healthz` and intercepts it with a 404 before the request
  reaches the container. `/health` works as expected.
- `--set-env-vars` syntax requires `^|^` delimiter when any value contains
  a comma (e.g. CORS origin list). This is documented but easy to miss.
- The browser auth flow ran twice: once for `gcloud auth application-default
  login` (Python SDK creds) and once for `gcloud auth login` (CLI creds).
  Both are required; they're separate stores.

## Cost projection

- BigQuery: ~50KB stored, <1MB queries/mo → free
- Cloud Storage: ~2MB → free (5GB/mo free tier)
- Cloud Run: min=0 means no idle cost; ~100 req/mo expected → free
- Cloud Build: 2 builds × ~3 min = 6 build-minutes → free (120 free)
- Artifact Registry: ~50MB image → free

**Estimated monthly: $0.00** (well under the $5/mo cap when set)

## What changed in the repo

- `notebooks/imm/api/main.py`: route renamed `/healthz` → `/health`
- `notebooks/imm/SETUP.md`: same rename in docs + cold-start note
- `work/imm-lab/index.html`: added `<script>window.IMM_API_URL = '...';</script>`
  before `imm-lab.js`, plus a small live-API badge in the §V controls
- `assets/js/imm-lab.js`: pings `/health` on load and surfaces status in
  the badge (predictions still run client-side; the badge is documentation)
- `assets/css/imm-lab.css`: badge styles
- `assets/data/imm-lab/{data,model}.json`: regenerated by the fresh PyMC
  fit on the user's Mac (overwrites prior posterior with new diagnostics)
