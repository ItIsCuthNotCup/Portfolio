# IMM Lab — Python training pipeline requirements
# Install with: pip install -r notebooks/imm/requirements.txt

numpy>=1.26
pandas>=2.1
pyarrow>=15.0

# Bayesian inference (Meridian preferred, PyMC fallback)
google-meridian>=1.0; python_version >= "3.10"
pymc>=5.10
arviz>=0.17

# GCP clients
google-cloud-bigquery>=3.17
google-cloud-storage>=2.14
db-dtypes>=1.2

# API server (Cloud Run)
fastapi>=0.110
uvicorn[standard]>=0.27
gunicorn>=21.2
pydantic>=2.6
